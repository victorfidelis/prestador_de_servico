{
  "compilerOptions": {
    // Apenas para informar ao VS Code a versão do JS que você está usando
    "target": "es2020",
    "module": "NodeNext",
    "moduleResolution": "nodenext", // Forma como o Node.js resolve módulos
    "allowSyntheticDefaultImports": true,
  },
  // Inclui todos os arquivos .js no projeto, exceto os de fora
  "include": [
    "**/*.js"
  ],
  "exclude": [
    "node_modules"
  ]
}